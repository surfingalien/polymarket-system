# Minimal requirements for Streamlit Cloud deployment.
# No Plotly — uses only Streamlit's built-in charts to guarantee
# compatibility across Python 3.10–3.14 and all Streamlit versions.
anthropic>=0.40.0
httpx>=0.27.0
tenacity>=8.3.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.4.0
structlog>=24.2.0
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0
streamlit>=1.35.0

# Live order signing (EIP-712). Lazily imported — the dashboard's demo/paper
# mode still works if this fails to install; only live execution needs it.
py-clob-client>=0.19.0
